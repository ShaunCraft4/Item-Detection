# Item-Detection
IF you decide to not download the "yolov8n.pt" file, please run the first commented line of code before running the main code. You might need to use the "pip install" command within the Python console to install some of the required modules.
