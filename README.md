# Item-Detection
IF you decide to not download the "yolov8n.pt" file, please run the first commented line of code before running the main code. Also, make sure to unzip the zipped files(this might take some time as these files contain the data required for training the AI model). You might need to use the "pip install" command within the Python console to install some of the required modules.
